node_modules/
client/dist/
server/data/*.db
server/data/*.db-shm
server/data/*.db-wal
server/data/db.json
# at-rest encryption key file for UI-saved API keys — NEVER commit
server/data/.veemp-secret
.veemp-secret
veemp-data/
*.log
.DS_Store
.env

# Optional local Python env for MarkItDown document ingestion (Phase 7).
.venv/
__pycache__/

# Local preview config
.claude/
server/data/demo-live.db*

# exe packaging artifacts (scripts/build-exe.mjs)
build/
dist-exe/

# Internal roadmap — kept local, not published
docs/FUTURE_PLAN.md

# Internal docs — kept local, not published
docs/
